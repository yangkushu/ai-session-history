# Cursor macOS Fixture

This directory is intentionally empty until latest Cursor on macOS is validated
with a real local sample. The Cursor reader currently reports unsupported
format for discovered `state.vscdb` files instead of parsing unverified storage.
